import 'package:flutter/material.dart';

void main() {
  runApp(const VotingDapp());
}

class VotingDapp extends StatelessWidget {
  const VotingDapp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
